[pytest]
# `python -m pytest` puts the working directory on sys.path but a bare
# `pytest` does not, so the app package would only import under one of the
# two. Declaring it here makes both work, locally and in CI.
pythonpath = .
testpaths = tests
